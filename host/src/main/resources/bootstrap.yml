spring:
  application:
    name: jitemizer # name must be used as filename in ConfigRepository that is read by ConfigServer
    version: v0.1
  cloud:
    config:
      uri: http://localhost:8888/ # URI to ConfigServer

message: Could not load configuration from ConfigServer # message that is loaded if connection to ConfigServer failed